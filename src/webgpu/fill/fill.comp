
layout(local_size_x = LOCAL_SIZE) in;

layout(set = 0, binding = 0) writeonly buffer Y {
    T[] y;
};

layout(push_constant) uniform PushConsts {
    T x;
};

void main() {
    uint index = gl_GlobalInvocationID.x;
    y[index] = x;
}


